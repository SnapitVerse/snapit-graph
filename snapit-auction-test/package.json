{
  "name": "snapit-auction-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ basarrcan/snapit-auction-test",
    "create-local": "graph create --node http://localhost:8020/ basarrcan/snapit-auction-test",
    "remove-local": "graph remove --node http://localhost:8020/ basarrcan/snapit-auction-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 basarrcan/snapit-auction-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
